CHATGPT PROJECT NOTES — REVIMED STATIC

Repo / branch:
- Work only in: /workspaces/revimedapp-static/revimed_app
- Live branch is: static-only
- Do NOT work on main unless explicitly asked.

Important workflow:
cd /workspaces/revimedapp-static/revimed_app
git checkout static-only
git pull origin static-only

Rules for utilaj pages:
- Do not duplicate content.
- If adding enhanced long SEO content + audio controls, hide or replace the original detail block so only one visible description remains.
- For utilaj pages, plain static HTML may be overwritten by hydration.
- Use post-hydration injector script for enhanced content/audio when needed.
- Keep existing hero/header/nav/footer.
- Make text readable, but not too large.

Rules for future edits:
- Preserve existing working content unless asked to replace it.
- Add only the needed files with git add, never git add .
- Ignore junk like node_modules/, .next/, out/, data/, revimedapp-static-work/.
- If many files are modified locally, commit only the exact file(s) requested.

For blogs:
- Keep favicon working.
- New and old blog pages should have the same audio/readability features when requested.
- Add a short bold intro line at the beginning when useful.

CONTACT CTA RULE:
All blog article pages and contact pages should end with a visible Revimed PLUS+ contact block:
- Revimed PLUS+ activează de peste 20 ani
- clinic phone: 022 60 50 60
- Dr. Igor Revenco, 35+ ani experiență
- doctor phone: 079 422 908
Do not duplicate this block. Replace existing REVIMED_CONTACT_CTA block if editing again.

UTILAJ SEO IMPORTANT:
For utilaj detail pages, do NOT use delayed injector for SEO text if avoidable.
Correct pattern:
- hide original short .equipmentDetailSection immediately with page CSS
- place the long SEO/audio block directly in static HTML after the original section
- keep only audio JS, not content-injection JS
This prevents 1-second delay and lets Google see the long text in initial HTML.

BLOG INDEX SEO IMPORTANT:
Blog index pages must be static HTML with all article cards present immediately.
Do NOT use delayed JS injection for blog cards.
If /blog first shows only the old 3 cards and later injects the rest, rebuild indexes:
python3 tools/rebuild_blog_indexes_static.py
Then commit:
public/blog/index.html
public/en/blog/index.html
public/ru/blog/index.html
public/ua/blog/index.html

BLOG INDEX DESIGN RULE:
The /blog index must be static but keep the original Revimed visual style:
- rmHeader / rmNav / rmFooter
- dark hero section
- cards using card blogCard style
- all article cards must exist directly in HTML
Do not rebuild /blog with a separate ugly custom layout.
Avoid delayed JS injection for blog cards.

BLOG INDEX ROLLBACK RULE:
Do NOT rebuild /blog index with custom static layout again.
Keep the original/injected Revimed blog index design because it matches the site.
If blog cards need updates, preserve the existing layout/classes and only update the content source/cards.
Do not replace /blog with a separate custom HTML design.

BLOG INDEX FINAL RULE:
User wants the old/injected-looking blog index layout, like the first 3 original blogs.
Do NOT replace /blog with custom static layout.
For /blog, preserve the old page layout/classes and only update the card list inside the existing grid.
New blogs must appear in that same old-style card grid.
From Home, Blog link may need hard navigation to /blog/ to avoid stale client-side cached version.

BLOG RECOVERY RULE:
Keep old/injected-style blog index layout.
Do not rebuild /blog with custom static design.
If new blogs are missing, only replace the card list inside the existing old grid:
<div class="rmShell grid3">...</div>
Do not replace blog article header/footer/main layout.
