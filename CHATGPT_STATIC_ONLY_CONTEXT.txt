REVIMED STATIC SITE — IMPORTANT CONTEXT

Repo:
https://github.com/dragoshelemenet/revimedapp-static

Work folder:
cd /workspaces/revimedapp-static/revimed_app

IMPORTANT:
Live site uses branch `static-only`.
Always work on `static-only`, NOT `main`.

Start every session:
git checkout static-only
git pull origin static-only

Deploy:
git push origin static-only

Do not commit junk folders:
node_modules/
.next/
out/
data/
revimedapp-static-work/

Sitemap:
Sitemap is static and must be updated directly:
sitemap.xml
public/sitemap.xml

Sitemap must NOT include:
/out
/public
/404
/_not-found
/not-found

Blogs:
When adding blogs, always:
1. Create real static article pages:
   public/.../blog/.../index.html
2. Add visible blog cards directly into these clean static blog index pages:
   public/blog/index.html
   public/en/blog/index.html
   public/ru/blog/index.html
   public/ua/blog/index.html
3. Do NOT rely only on old Next.js embedded script payloads. The blog index pages were rebuilt as clean static HTML so new cards must be inserted in the visible HTML.
4. Regenerate sitemap.xml and public/sitemap.xml
5. Use all 4 languages: RO, EN, RU, UA
6. Keep language simple, human, useful, not GPT-like
7. Use SEO topics from real patient searches
8. Mention naturally:
   - Revimed PLUS+ founded in 2005
   - doctor has 35+ years experience
   - treatment is selected individually
   - patient can choose classic medical treatment or gentler/naturopathic direction when safe
   - everything is discussed and done with patient agreement
   - some patients improve with physiotherapy/gentler methods, others need medication
   - medication can be adjusted to find a suitable option with fewer side effects when possible
9. Do not overpromise or say “best clinic”. Explain calmly why Revimed is a good choice.

Recent important fix:
Blog index pages were rebuilt as clean static HTML and pushed on static-only:
commit 7705b2e
Message: Rebuild blog index pages as static HTML

Clean workflow:
git status
git add ONLY needed files
git commit -m "Clear message"
git push origin static-only

Never use `git add .` unless checked carefully.
